dist: trusty
language: php
php:
  - '5.6'
  - '7.0'
  - hhvm # on Trusty only
  - nightly

script:
    - find {src,tests} -name "*.php"  -print0 \
      | xargs -0 -n1 -P8 php -l \
      | grep -v '^No syntax errors detected' \
      ; test $? -eq 1
